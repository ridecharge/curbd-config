{
  "bootstrap-configuration-jobs": {
    "name": "Bootstrap Configuration Jobs",
    "description": "These bootstrap jenkins and create the initial job templates.",
    "regex": ".+-jobs$"
  },
  "view-configuration-jobs": {
    "name": "View Configuration Jobs",
    "description": "These bootstrap jenkins and create the initial job templates.",
    "regex": "^generate-.+-views$"
  },
  "jenkins-configuration-jobs": {
    "name": "Jenkins Configuration Jobs",
    "description": "These jobs configure the jenkins instance.",
    "regex": "^jenkins-.+-config$"
  },
  "curbformation-deploy-jobs": {
    "name": "Curbformation Deploy Jobs",
    "description": "These jobs manage cloudformation deployments.",
    "regex": "^curbformation-templates-.+-.+$"
  },
  "curbd-populate-jobs": {
    "name": "Curbd Populate Jobs",
    "description": "These jobs manage configuration in consul.",
    "regex": "^curbd-.+-populate$"
  }
}
